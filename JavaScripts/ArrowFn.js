//we can use any of the below set of code

// function add(a,b){
//     return (a + b);
// }

// add = (a,b) => {
//     return (a + b);
// }

add = (a,b) => a + b;

let result = add(10,5);
console.log(result);


//add = () =>  10 + 10; //If only No parameter
//add = (a) => a + 10; //If only one parameter. bracket is optional
//add = (a,b) => a + b; //If more than one parameter